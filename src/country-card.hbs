{{#each this}}
<div>
    <h1>{{name}}</h1>
    <img src="{{flag}}" alt="{{name}}" width="200" height="200">

    <p>Capital: {{capital}}</p>
    <p>Population: {{population}}</p>
    {{#each languages}}
    <ul>Languages:
        <li>{{this.name}}</li>
        {{/each}}
    </ul>

</div>
{{/each}}